features:
    ☐ background color from LDtk world settings

    importer settings to get rid of extension classes:
        ☐ entity path like res://enities/{}.tscn
        ☐ base scene
        ☐ levels parent node

    ☐ if plugin finds type with LDtkEntity attribute it instances that scene

    ☐ LDtkFieldAttribute

    ☐ LDtkEntityAttribute

    ☐ use YSort for entity layer if it has special tag

    json in tile custom data:
        ☐ entity field to replace tile with entity
        ☐ scene field to replace tile with any scene
        ☐ other fields for LDtkFieldAttribute

    ☐ keep collision in TileSet resource
